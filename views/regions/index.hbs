<h1>Regiones</h1>
<a href="/regions/create" class="btn btn-success mb-3">Crear nueva región</a>
<div class="row">
    {{#each regions}}
        <div class="col-md-4 mb-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{this.name}}</h5>
                    <a href="/regions/edit/{{this.id}}" class="btn btn-primary">Editar</a>
                    <a href="/regions/delete/{{this.id}}" class="btn btn-danger">Eliminar</a>
                </div>
            </div>
        </div>
    {{/each}}
</div>